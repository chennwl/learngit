vi <fileName>  可以在shell编辑某个文件,编辑完成后可连按两次Z退出
cat <fileName> 可以在shell查看某个文件

在window下git push不需要每次都输入账号密码：
git remote rm origin
git remote add origin git@github.com:username/repository.git
git push -u origin master
